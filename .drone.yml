---
kind: pipeline
type: docker
name: default

steps:
- name: parcel_build
  image: node
  commands:
    - npm install
    - npx --max-old-space-size=768 parcel build index.html --log-level verbose
- name: docker_build
  image: plugins/docker
  settings:
    repo:  registry.digitalocean.com/mochilo-container-registry/cavegame
    username:
      from_secret: docker_username
    password:
      from_secret: docker_password
    registry:  registry.digitalocean.com
    tags: latest
